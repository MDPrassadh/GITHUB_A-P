name: Python name change Versions Testing
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        os: [3.8, 3.9, 3.10]
    steps:
      - run: echo "Testing on Python ${{ matrix.os }}"

#Use case: compatibility testing.

