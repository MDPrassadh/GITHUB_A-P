name: max-parallel 1 Control

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 1
      fail-fast: false
      matrix:
        version: [1,2,3,4,5]
    steps:
      - run: echo "Version = ${{ matrix.version }}"

#Use case: limit runner load, run all jobs even if one fails.